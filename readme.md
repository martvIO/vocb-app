the goal of this project is to create an app for both ipad and desktop, that runs on the background, that when the user select a text a pop up show that insert this word in the database and generate a list of vocabs for that words with examples, have flash cards, and count how many times this word acouerd overall and sort them and generate a flash card or any type of test to test the user, and also show an overlay on the screen with word meaning when the user select it