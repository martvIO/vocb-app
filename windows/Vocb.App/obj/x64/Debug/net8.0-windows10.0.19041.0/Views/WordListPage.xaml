<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Vocb.App.Views.WordListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="using:Vocb.Core">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Words" FontSize="28" FontWeight="SemiBold" />
        <TextBlock x:ConnectionId='2' x:Name="StatusText" Grid.Row="1" Foreground="Gray" />

        <ListView x:ConnectionId='3' Grid.Row="2" x:Name="WordsList">
            <ListView.ItemTemplate>
                <DataTemplate                            >
                    <Grid x:ConnectionId='5' ColumnSpacing="12" Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock x:ConnectionId='7'                      FontWeight="SemiBold" FontSize="16" />
                            <TextBlock x:ConnectionId='8'                                   TextWrapping="Wrap" Foreground="Gray" />
                        </StackPanel>
                        <Border Grid.Column="1" VerticalAlignment="Center"
                                Background="{ThemeResource AccentFillColorDefaultBrush}"
                                CornerRadius="10" Padding="8,2">
                            <TextBlock x:ConnectionId='6'                             Foreground="White" />
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

